#include <iostream>
#include <vector>
#include <string>
#include"Header.h"
using namespace std;

//Pasos
//1.Piezas
//2.Imprimir

//cosas que arreglar
//tabulador en la primera fila para que esten cuadrados

void main() {
	inicialPieces();
	printTable();
	
	/*
	* bool gameOver = false;
	bool playerWhite=true, playerBlack=false;
	char insertPiece;
	short x, y;
	* 	while (!gameOver) {
		if (playerWhite == true) {
			cout << "Pieza que quieres mover?"<<endl;
			cin >> insertPiece;
			selectPieces(insertPiece);
			//limitar randgo de casillas
			// de pende de que pieza tiene un determinado movimiento
			cout << "que casilla la quieres mover (x enter y)";
			cin >> x;
			cin >> y;
			movePieces(insertPiece, x, y);
		}
		//para turnar los juagadores
		playerWhite ? false : true;
		playerBlack ? false : true;
		//condiciones del rey muerto
		//condiciones de la pieza muerta
	}
	*/

}